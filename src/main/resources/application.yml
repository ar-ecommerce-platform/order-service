---
spring:
  application:
    name: order-service
  flyway:
    enabled: false   # dev runs on H2 + ddl-auto; the 'prod' profile enables Flyway
  datasource:
    url: jdbc:h2:mem:orderdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true   # dev only - lets the browser reach the in-container console

server:
  port: ${SERVER_PORT:8082}

downstream:
  product-service: ${PRODUCT_SERVICE_URL:lb://product-service}
  inventory-service: ${INVENTORY_SERVICE_URL:lb://inventory-service}
  payment-service: ${PAYMENT_SERVICE_URL:lb://payment-service}
  notification-service: ${NOTIFICATION_SERVICE_URL:lb://notification-service}

management:
  endpoints:
    web:
      exposure:
        include: health,info

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: true
